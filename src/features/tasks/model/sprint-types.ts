/** @deprecated Import from `@/features/sprints` — temporary shim. */
export type {
    BoardSprintScope,
    Sprint,
    SprintEvent,
    SprintEventType,
    SprintState,
} from "@/features/sprints/model/types";
