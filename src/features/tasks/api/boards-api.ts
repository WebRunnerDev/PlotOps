/** @deprecated Import from `@/features/boards` — temporary shim. */
export {
    fetchBoardColumnIds,
    fetchBoardColumnSummaries as fetchBoardColumns,
} from "@/features/boards/api/board-columns-api";
/** @deprecated Import from `@/features/boards` — temporary shim. */
export {
    boardHasTasks,
    createBoard,
    deleteBoard,
    fetchBoard,
    fetchProjectBoards,
    updateBoard,
} from "@/features/boards/api/boards-api";
/** @deprecated Import from `@/features/boards` — temporary shim. */
export type { ProjectBoardRecord } from "@/features/boards/model/types";
