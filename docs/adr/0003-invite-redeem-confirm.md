# Invite redeem: email match or Owner/Admin confirm

An Invite is accepted when the signed-in user's email matches the Invite email. If it does not (common with GitHub noreply addresses), an Owner or Admin may manually confirm that this account fulfills the Invite. Token-only redeem without email checks was rejected — shared links stay bound to an intended person, with a human escape hatch for real use with close collaborators.
